#!/usr/bin/execlineb -P
envfile /etc/s6/config/fwknop.conf
importas -sCiu DIRECTIVES DIRECTIVES
ifelse { test -w /var/log } {
foreground { install -d -o s6log -g s6log /var/log/fwknopd }
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /var/log/fwknopd
}
foreground { install -d -o s6log -g s6log /run/log/fwknopd }
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /run/log/fwknopd
