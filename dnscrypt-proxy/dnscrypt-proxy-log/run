#!/usr/bin/execlineb -P
envfile /etc/s6/config/dnscrypt-proxy.conf
importas -sCiu DIRECTIVES DIRECTIVES
ifelse { test -w /var/log } {
	foreground { install -d -o s6log -g s6log /var/log/dnscrypt-proxy }
	s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /var/log/dnscrypt-proxy
}
foreground { install -d -o s6log -g s6log /run/log/dnscrypt-proxy }
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /run/log/dnscrypt-proxy
