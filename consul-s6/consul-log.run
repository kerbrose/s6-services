#!/usr/bin/execlineb -P
foreground { if -n -t { test -d /var/log/consul } install -d -m 0755 -o s6log -g s6log /var/log/consul }
foreground { chmod -R 755 /var/log/consul }
envfile /etc/s6/sv/consul-log/conf
importas -sCiu DIRECTIVES DIRECTIVES
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /var/log/consul
