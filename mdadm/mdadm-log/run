#!/usr/bin/execlineb -P
foreground { if -n -t { test -d /var/log/mdadm } install -d -m 0755 -o s6log -g s6log /var/log/mdadm }
envfile /etc/s6/sv/mdadm-log/conf
importas -sCiu DIRECTIVES DIRECTIVES
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /var/log/mdadm
