#!/usr/bin/execlineb -P
envfile /etc/s6/config/docker.conf
importas -sCiu DIRECTIVES DIRECTIVES
ifelse { test -w /var/log } {
foreground { if -n -t { test -d /var/log/docker } install -d -m 0755 -o s6log -g s6log /var/log/docker }
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /var/log/docker
}
foreground { install -d -o s6log -g s6log /run/log/docker }
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /run/log/docker
