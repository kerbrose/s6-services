#!/usr/bin/execlineb -P
foreground { if -n -t { test -d /var/log/fail2ban } install -d -m 0755 -o s6log -g s6log /var/log/fail2ban }
foreground { chmod -R 755 /var/log/fail2ban }
envfile /etc/s6/config/fail2ban.conf
importas -sCiu DIRECTIVES DIRECTIVES
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /var/log/fail2ban
