#!/usr/bin/execlineb -P
foreground { if -n -t { test -d /var/log/dnsmasq } install -d -m 0755 -o s6log -g s6log /var/log/dnsmasq }
envfile /etc/s6/config/dnsmasq.conf
importas -sCiu DIRECTIVES DIRECTIVES
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /var/log/dnsmasq
