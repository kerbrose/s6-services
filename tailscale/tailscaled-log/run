#!/usr/bin/execlineb -P
envfile /etc/s6/config/tailscale.conf
importas -sCiu DIRECTIVES DIRECTIVES
ifelse { test -w /var/log } {
	foreground { install -d -o s6log -g s6log /var/log/tailscaled }
	s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /var/log/tailscaled
}
foreground { install -d -o s6log -g s6log /run/log/tailscaled }
s6-setuidgid s6log exec -c s6-log -d3 -b -- ${DIRECTIVES} /run/log/tailscaled
